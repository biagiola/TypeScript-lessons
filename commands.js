npm install - g typescript; // to install
tsc; // see all the commands.
tsc app.ts; // in current directory transpile ts file into js.
tsc - w apps.ts; // transpile in the watch mode.
tsc apps.ts && node apps.js // runs the two commands at the same time
